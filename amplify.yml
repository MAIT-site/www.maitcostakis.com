version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Building site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
    exclude:
      - gr/**/*
      - 'index-old*.html'
  cache:
    paths: []
